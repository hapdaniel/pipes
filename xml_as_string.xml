<?xml version="1.0" encoding="UTF-8"?>
<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
    <meta>
        <author>Paul Daniel via Paul Donnelly</author>
	<description>Makes the XML results into a string. Pretty useless?</description>
    </meta>
    <bindings>
        <select itemPath="" produces="XML">
            <urls>

                <url></url>
            </urls>
            <inputs>
                <key id="url" type="xs:string" paramType="variable" required="true"/>
            </inputs>
            <execute><![CDATA[
		var q = y.query('select * from xml where url=@url',{url:url});
                var myresults = q.results;
                response.object = myresults.toString();
            ]]>
            </execute>
        </select>

     </bindings>
</table>
